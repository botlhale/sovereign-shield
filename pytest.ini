[pytest]
testpaths = tests
pythonpath = src tests
addopts = -q --strict-markers
markers =
    live: requires a reachable Databricks workspace; opt in with --live
    stress: high-volume benchmark; opt in with --stress
